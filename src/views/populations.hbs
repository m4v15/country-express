<header>
  <h1 class="">{{title}}</h1>
  <p>Hello {{username}}!</p>
</header>
<ul id="" class="">
  {{#each countries}} {{#if this.population}}
  <li class="">{{this.country}} has a population of {{this.population}}</li>
  {{/if}} {{/each}}
</ul>